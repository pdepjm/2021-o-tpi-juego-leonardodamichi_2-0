import wollok.game.*
import pepita.*
import texto.*
import grilla.*

program juego {
	game.title("Batalla Naval")
	game.width(32)
  	game.height(18)
  	game.cellSize(40)
	game.addVisual(grilla)
	game.addVisual(pepita)
	game.addVisual(texto)
	game.boardGround("fondoNaval.jpg")
	game.start()
}